<Window x:Class="CardGameScorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CardGameScorer"
        xmlns:vm="clr-namespace:CardGameScorer.ViewModels"
        xmlns:conv="clr-namespace:CardGameScorer.Converters"
        mc:Ignorable="d"
        Title="Barbu Scorer" Height="750" Width="1000"
        Icon="Assets/BarbuPhoto.png"
        WindowState="Maximized"
        Background="#1e1e2e"
        FontSize="{Binding BaseFontSize}"
        PreviewKeyDown="Window_PreviewKeyDown">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
        <conv:TabSelectedConverter x:Key="TabSelectedConverter"/>
        <conv:TabVisibilityConverter x:Key="TabVisibilityConverter"/>
        <conv:EnumBoolConverter x:Key="EnumBoolConverter"/>
        <conv:BoolToColorConverter x:Key="BoolToColorConverter"/>
        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#89b4fa"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#45475a"/>
                    <Setter Property="Foreground" Value="#6c7086"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SettingsTabButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="content" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#89b4fa"/>
                                <Setter Property="Foreground" Value="#1e1e2e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsEditButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#585b70"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#89b4fa"/>
                                <Setter Property="Foreground" Value="#1e1e2e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#e6e9ef"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#1e1e2e"/>
        </Style>
        <Style x:Key="ToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#6c7086"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5c6bc0"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#5c6bc0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5c6bc0"/>
                                <Setter Property="Foreground" Value="#1e1e2e"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#5c6bc0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#313244"/>
                                <Setter Property="Foreground" Value="#6c7086"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#45475a"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <!-- ==================== PLAYER SETUP SCREEN ==================== -->
        <Grid Visibility="{Binding IsSetupScreen, Converter={StaticResource BoolToVis}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border Background="#313244" CornerRadius="15" Padding="40" 
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    MinWidth="500" Margin="20">
                <StackPanel>
                    <Image Source="Assets/BarbuPhoto.png" Width="120" Height="120" 
                           HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="Barbu Scorer" FontSize="{Binding TitleFontSize}" FontWeight="Bold" 
                               Foreground="#f5c2e7" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Enter player names for each position" 
                               Foreground="#a6adc8" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                    
                    <!-- Game name input -->
                    <Grid Margin="0,0,0,25">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Game Name:" FontWeight="SemiBold" 
                                   VerticalAlignment="Center" Foreground="#a6e3a1"/>
                        <TextBox Grid.Column="1" Text="{Binding GameName, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,0,0"/>
                    </Grid>
                    
                    <!-- Player name inputs -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="15"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="15"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="15"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="West:" FontWeight="SemiBold" 
                                   VerticalAlignment="Center" Foreground="#f9e2af"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding WestName, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,0,0"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="North:" FontWeight="SemiBold" 
                                   VerticalAlignment="Center" Foreground="#f9e2af"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NorthName, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,0,0"/>
                        
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="East:" FontWeight="SemiBold" 
                                   VerticalAlignment="Center" Foreground="#f9e2af"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding EastName, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,0,0"/>
                        
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="South:" FontWeight="SemiBold" 
                                   VerticalAlignment="Center" Foreground="#f9e2af"/>
                        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding SouthName, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,0,0"/>
                    </Grid>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button Content="Start Game" Command="{Binding StartGameCommand}" 
                                Margin="0,0,15,0"
                                IsEnabled="{Binding CanStartGame}"/>
                        <Button Content="Load Game" Command="{Binding LoadGameCommand}"
                                Background="#6c7086" Margin="0,0,15,0"/>
                        <Button Content="Game Settings" Command="{Binding OpenGameSettingsCommand}"
                                Background="#585b70"/>
                    </StackPanel>
                    
                    <TextBlock Text="First round is West's game. Bidding starts with North (clockwise)." 
                               Foreground="#6c7086" HorizontalAlignment="Center" Margin="0,20,0,0"
                               FontSize="{Binding MediumFontSize}"/>
                </StackPanel>
            </Border>
            </ScrollViewer>
        </Grid>
        
        <!-- ==================== GAME PLAY SCREEN ==================== -->
        <Grid Visibility="{Binding IsGameScreen, Converter={StaticResource BoolToVis}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="Barbu Scorer" FontSize="{Binding HugeFontSize}" FontWeight="Bold" 
                               Foreground="#89b4fa" HorizontalAlignment="Center"/>
                    
                    <!-- Error Message -->
                    <Border Background="#f38ba8" CornerRadius="5" Padding="10" Margin="0,10,0,0"
                            Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#1e1e2e" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
                
                <!-- Settings Gear Button -->
                <Grid Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button Command="{Binding ToggleSettingsMenuCommand}" 
                            BorderThickness="0"
                            Padding="10" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="5">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#5c6bc0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="⚙" FontSize="24" Foreground="#cdd6f4"/>
                    </Button>
                    
                    <!-- Settings Popup Menu -->
                    <Popup IsOpen="{Binding IsSettingsMenuOpen}" 
                           PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=Button}}"
                           Placement="Bottom" StaysOpen="False"
                           AllowsTransparency="True">
                        <Border Background="#313244" CornerRadius="8" Padding="5" 
                                BorderBrush="#45475a" BorderThickness="1">
                            <StackPanel MinWidth="150">
                                <Button Content="Save Game" Command="{Binding SaveGameCommand}"
                                        HorizontalContentAlignment="Left" Padding="10,8"
                                        BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="#cdd6f4"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#89b4fa"/>
                                                    <Setter Property="Foreground" Value="#1e1e2e"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="Load Game" Command="{Binding LoadGameCommand}"
                                        HorizontalContentAlignment="Left" Padding="10,8"
                                        BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="#cdd6f4"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#89b4fa"/>
                                                    <Setter Property="Foreground" Value="#1e1e2e"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Border Height="1" Background="#45475a" Margin="5,3"/>
                                <Button Content="New Game" Command="{Binding NewGameCommand}"
                                        HorizontalContentAlignment="Left" Padding="10,8"
                                        BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="#f38ba8"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#89b4fa"/>
                                                    <Setter Property="Foreground" Value="#a63d56"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Border Height="1" Background="#45475a" Margin="5,3"/>
                                <Button Content="Game Settings" Command="{Binding OpenGameSettingsCommand}"
                                        HorizontalContentAlignment="Left" Padding="10,8"
                                        BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="#cdd6f4"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#89b4fa"/>
                                                    <Setter Property="Foreground" Value="#1e1e2e"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>
            </Grid>
            
            <!-- Tab Control -->
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0"
                        Visibility="{Binding IsSettingsOpen, Converter={StaticResource InverseBoolToVis}}">
                <TabControl.Style>
                    <Style TargetType="TabControl">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabControl">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <TabPanel Grid.Row="0" IsItemsHost="True" HorizontalAlignment="Center"/>
                                        <ContentPresenter Grid.Row="1" ContentSource="SelectedContent"/>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Style>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Background" Value="#313244"/>
                        <Setter Property="Foreground" Value="#cdd6f4"/>
                        <Setter Property="Padding" Value="20,10"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Border" Background="{TemplateBinding Background}" 
                                            CornerRadius="8,8,0,0" Padding="{TemplateBinding Padding}" Margin="0,0,5,0">
                                        <ContentPresenter ContentSource="Header"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#89b4fa"/>
                                            <Setter Property="Foreground" Value="#1e1e2e"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#45475a"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                                <Condition Property="IsSelected" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Border" Property="Background" Value="#89b4fa"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                
                <!-- ==================== TAB 1: CURRENT GAME ==================== -->
                <TabItem Header="Current Game">
                    <Border Background="#1e1e2e" Padding="0,15,0,0" MaxWidth="850" HorizontalAlignment="Center">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <!-- Player Scores -->
                                <TextBlock Text="Player Scores" FontSize="{Binding XXLargeFontSize}" FontWeight="Bold" 
                                           Foreground="#89b4fa" Margin="0,0,0,15"/>
                                
                                <ItemsControl ItemsSource="{Binding Players}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Rows="1"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#313244" CornerRadius="10" Padding="15" Margin="0,0,10,15">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}" FontSize="{Binding DataContext.XXLargeFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" Foreground="#89b4fa"/>
                                                    <TextBlock Text="{Binding PositionName}" Foreground="#a6adc8" FontSize="{Binding DataContext.MediumFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                    <TextBlock Text="{Binding TotalScore}" FontSize="{Binding DataContext.TitleFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" 
                                                               Foreground="#a6e3a1" Margin="0,10,0,0"/>
                                                    <TextBlock Foreground="#6c7086" FontSize="{Binding DataContext.SmallFontSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,5,0,0">
                                                        <Run Text="{Binding RemainingContracts.Count, Mode=OneWay}"/>
                                                        <Run Text=" games remaining"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <!-- Dealer Doubles Matrix -->
                                <TextBlock Text="Dealer Doubles Tracker" FontSize="{Binding XXLargeFontSize}" FontWeight="Bold" 
                                           Foreground="#89b4fa" Margin="0,20,0,10"/>
                                <TextBlock Text="Track doubles: Each player must double each dealer at least twice during the game." 
                                           Foreground="#a6adc8" FontSize="{Binding MediumFontSize}" Margin="0,0,0,15"/>
                                
                                <Border Background="#313244" CornerRadius="10" Padding="15" HorizontalAlignment="Center">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Header Row -->
                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Width="175" FontWeight="SemiBold" FontSize="{Binding BaseFontSize}"><Run Text="Doubler ↓ / " Foreground="#f9e2af"/><Run Text="Dealer →" Foreground="#89b4fa"/></TextBlock>
                                            <ItemsControl ItemsSource="{Binding Players}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Name}" Width="100" FontWeight="SemiBold" 
                                                                   TextAlignment="Center" Foreground="#89b4fa" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                        
                                        <!-- Data Rows -->
                                        <ItemsControl Grid.Row="1" ItemsSource="{Binding DealerDoubleMatrix}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                        <TextBlock Text="{Binding Doubler.Name}" Width="175" VerticalAlignment="Center" Foreground="#f9e2af" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                        <ItemsControl ItemsSource="{Binding DoubleCounts}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <StackPanel Orientation="Horizontal"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Border Width="100" Padding="5" Margin="0,0,0,0">
                                                                        <TextBlock Text="{Binding DisplayText}" TextAlignment="Center"
                                                                                   FontWeight="SemiBold" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Foreground" Value="#f38ba8"/>
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding ColorCategory}" Value="1">
                                                                                            <Setter Property="Foreground" Value="#f9e2af"/>
                                                                                        </DataTrigger>
                                                                                        <DataTrigger Binding="{Binding ColorCategory}" Value="2">
                                                                                            <Setter Property="Foreground" Value="#a6e3a1"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </TabItem>
                
                <!-- ==================== TAB 2: SCORING ==================== -->
                <TabItem Header="Scoring">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Border Background="#1e1e2e" Padding="0,15,0,0" MaxWidth="850" HorizontalAlignment="Center">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Status Message -->
                            <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" Foreground="#a6adc8" 
                                       TextWrapping="Wrap" Margin="0,0,0,15"/>
                            
                            <!-- Main Content -->
                            <Grid Grid.Row="1">
                
                <!-- Game Flow Panel -->
                <Border Background="#313244" CornerRadius="10" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Phase: Selecting Contract -->
                        <StackPanel Grid.Row="0" Grid.RowSpan="2"
                                    Visibility="{Binding IsSelectingContract, Converter={StaticResource BoolToVis}}">
                            <TextBlock FontSize="{Binding XLargeFontSize}" Foreground="#89b4fa" Margin="0,0,0,20">
                                <Run Text="{Binding DealerNamePossessive, Mode=OneWay}" FontWeight="Bold"/>
                                <Run Text="{Binding DealerTurnSuffix, Mode=OneWay}" FontWeight="SemiBold"/>
                            </TextBlock>
                            
                            <TextBlock Text="Select a game:" Margin="0,0,0,12"/>
                            
                            <ItemsControl ItemsSource="{Binding AllContractOptions}" Margin="0,0,0,15">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,5" Padding="10,8" CornerRadius="6">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#313244"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="Background" Value="#45475a"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="140"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Clickable Game Name -->
                                                <Button Grid.Column="0"
                                                        Command="{Binding DataContext.SelectContractCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"
                                                        IsEnabled="{Binding IsAvailable}"
                                                        HorizontalContentAlignment="Left" Padding="0"
                                                        Cursor="Hand">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <ContentPresenter/>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="#89b4fa"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                                                                        <Setter Property="Foreground" Value="#6c7086"/>
                                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                        <Setter Property="Foreground" Value="#a6e3a1"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Button>
                                                
                                                <!-- Description -->
                                                <TextBlock Grid.Column="1" Text="{Binding Description}" 
                                                           Foreground="#a6adc8" FontSize="{Binding DataContext.MediumFontSize, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                           VerticalAlignment="Center"/>
                                                
                                                <!-- Selected Indicator -->
                                                <TextBlock Grid.Column="2" Text="✓" FontSize="{Binding DataContext.LargeFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold"
                                                           Foreground="#a6e3a1" VerticalAlignment="Center" Margin="10,0,0,0"
                                                           Visibility="{Binding IsSelected, Converter={StaticResource BoolToVis}}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <TextBlock Text="{Binding ContractDescription}" Foreground="#a6adc8" 
                                       FontStyle="Italic" Margin="0,0,0,20"/>
                            
                            <Button Content="Start Bid" Command="{Binding StartBidCommand}" 
                                    IsEnabled="{Binding CanStartBid}" HorizontalAlignment="Left"/>
                        </StackPanel>
                        
                        <!-- Phase: Bidding (Doubling) -->
                        <StackPanel Grid.Row="0" Grid.RowSpan="2"
                                    Visibility="{Binding IsBidding, Converter={StaticResource BoolToVis}}">
                            
                            <!-- Doubling Phase -->
                            <StackPanel Visibility="{Binding IsInDoublingPhase, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{Binding CurrentBiddingPlayerDisplay}" FontSize="{Binding XLargeFontSize}" 
                                           FontWeight="SemiBold" Foreground="#f9e2af" Margin="0,0,0,20"/>
                                
                                <TextBlock Text="Choose players to double (or skip):" Margin="0,0,0,15"/>
                                
                                <ItemsControl ItemsSource="{Binding DoubleTargets}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,10,10">
                                                <ToggleButton Style="{StaticResource ToggleButtonStyle}"
                                                              IsEnabled="{Binding IsClickable}"
                                                              IsChecked="{Binding IsSelected}"
                                                              Click="DoubleTarget_Click" Tag="{Binding}"
                                                              MinWidth="120">
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Player.Name}" FontSize="{Binding DataContext.LargeFontSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                   HorizontalAlignment="Center"/>
                                                        <TextBlock Text="Dealer" FontSize="{Binding DataContext.SmallFontSize, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#f9e2af"
                                                                   HorizontalAlignment="Center"
                                                                   Visibility="{Binding IsDealer, Converter={StaticResource BoolToVis}}"/>
                                                        <TextBlock Text="{Binding StatusText}" 
                                                                   FontSize="{Binding DataContext.SmallFontSize, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                                   FontWeight="Bold"
                                                                   HorizontalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="#f38ba8"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsMandatory}" Value="True">
                                                                            <Setter Property="Foreground" Value="#fab387"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </ToggleButton>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <StackPanel Margin="0,20,0,0">
                                    <Button Content="MAX" Command="{Binding MaxDoublesCommand}" 
                                            Background="#fab387" Foreground="#1e1e2e" Margin="0,0,0,10"
                                            HorizontalAlignment="Center"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,50" HorizontalAlignment="Center">
                                        <Button Content="Skip" Command="{Binding SkipDoubleCommand}" 
                                                Background="#6c7086" Margin="0,0,10,0"/>
                                        <Button Content="Confirm" Command="{Binding ConfirmDoublesCommand}"/>
                                    </StackPanel>
                                    <Button Content="Back" Command="{Binding BackBiddingStepCommand}" 
                                            Background="#6c7086" HorizontalAlignment="Left"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- Immediate Redouble Response -->
                            <StackPanel Visibility="{Binding IsWaitingForImmediateRedouble, Converter={StaticResource BoolToVis}}">
                                <Border Background="#45475a" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                                    <StackPanel>
                                        <TextBlock Text="{Binding ImmediateRedoubleDisplay}" FontSize="{Binding XLargeFontSize}" 
                                                   FontWeight="SemiBold" Foreground="#fab387" 
                                                   TextWrapping="Wrap" Margin="0,0,0,20"/>
                                        
                                        <TextBlock Text="Do you want to redouble?" Margin="0,0,0,15"/>
                                        
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Yes, Redouble!" 
                                                    Command="{Binding AcceptRedoubleCommand}" 
                                                    Background="#fab387" Margin="0,0,15,0"
                                                    Padding="25,12"/>
                                            <Button Content="No, Decline" 
                                                    Command="{Binding DeclineRedoubleCommand}" 
                                                    Background="#6c7086"
                                                    Padding="25,12"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                        
                        <!-- Phase: Entering Scores -->
                        <StackPanel Grid.Row="0" Grid.RowSpan="2"
                                    Visibility="{Binding IsEnteringScores, Converter={StaticResource BoolToVis}}">
                            <TextBlock FontSize="{Binding XLargeFontSize}" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="#a6e3a1">
                                <Run Text="Enter Scores for "/><Run Text="{Binding SelectedContractName, Mode=OneWay}"/>
                            </TextBlock>
                            
                            <!-- Doubles Summary -->
                            <Border Background="#45475a" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="Doubles for this round:" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                    <ItemsControl ItemsSource="{Binding BiddingState.Doubles}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                                    <TextBlock Foreground="#cba6f7">
                                                        <Run Text="{Binding Doubler.Name, Mode=OneWay}"/>
                                                        <Run Text=" doubled "/>
                                                        <Run Text="{Binding Target.Name, Mode=OneWay}"/>
                                                    </TextBlock>
                                                    <TextBlock Text=" (redoubled)" Foreground="#f38ba8"
                                                               Visibility="{Binding IsRedoubled, Converter={StaticResource BoolToVis}}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="No doubles" Foreground="#6c7086" 
                                               Visibility="{Binding BiddingState.HasNoDoubles, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Ace of Hearts dropdown (only for Hearts contract) -->
                            <StackPanel Visibility="{Binding IsHeartsContract, Converter={StaticResource BoolToVis}}"
                                        Margin="0,0,0,15">
                                <TextBlock Text="Player who won the Ace of Hearts:" FontWeight="SemiBold" 
                                           Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding Players}" 
                                          SelectedItem="{Binding AceOfHeartsPlayer}"
                                          DisplayMemberPath="DisplayName"
                                          Width="200" HorizontalAlignment="Left"
                                          Background="#e6e9ef"
                                          Foreground="#1e1e2e"/>
                            </StackPanel>
                            
                            <!-- King of Hearts dropdown (only for Barbu contract) -->
                            <StackPanel Visibility="{Binding IsBarbuContract, Converter={StaticResource BoolToVis}}"
                                        Margin="0,0,0,15">
                                <TextBlock Text="Select who took the King of Hearts" FontSize="{Binding LargeFontSize}" 
                                           FontWeight="Bold" Foreground="#228B22" Margin="0,0,0,15" TextWrapping="Wrap"/>
                                <TextBlock Text="Player who won the King of Hearts:" FontWeight="SemiBold" 
                                           Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding Players}" 
                                          SelectedItem="{Binding KingOfHeartsPlayer}"
                                          DisplayMemberPath="DisplayName"
                                          Width="200" HorizontalAlignment="Left"
                                          Background="#e6e9ef"
                                          Foreground="#1e1e2e"/>
                            </StackPanel>
                            
                            <!-- No Last Two dropdowns -->
                            <StackPanel Visibility="{Binding IsNoLastTwoContract, Converter={StaticResource BoolToVis}}"
                                        Margin="0,0,0,15">
                                <TextBlock Text="Select who won the last two tricks" FontSize="{Binding LargeFontSize}" 
                                           FontWeight="Bold" Foreground="#228B22" Margin="0,0,0,15" TextWrapping="Wrap"/>
                                <TextBlock Text="Player who won the 2nd to last trick:" FontWeight="SemiBold" 
                                           Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding Players}" 
                                          SelectedItem="{Binding SecondToLastTrickPlayer}"
                                          DisplayMemberPath="DisplayName"
                                          Width="200" HorizontalAlignment="Left"
                                          Background="#e6e9ef"
                                          Foreground="#1e1e2e"
                                          Margin="0,0,0,10"/>
                                <TextBlock Text="Player who won the last trick:" FontWeight="SemiBold" 
                                           Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding Players}" 
                                          SelectedItem="{Binding LastTrickPlayer}"
                                          DisplayMemberPath="DisplayName"
                                          Width="200" HorizontalAlignment="Left"
                                          Background="#e6e9ef"
                                          Foreground="#1e1e2e"/>
                            </StackPanel>
                            
                            <!-- Ravage City player selection (checkboxes for ties) -->
                            <StackPanel Visibility="{Binding IsRavageCityContract, Converter={StaticResource BoolToVis}}"
                                        Margin="0,0,0,15">
                                <TextBlock Text="Select the player(s) who took the most cards in any suit" 
                                           FontSize="{Binding LargeFontSize}" 
                                           FontWeight="Bold" Foreground="#228B22" Margin="0,0,0,15" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding RavageCityScoringDescription}" 
                                           Foreground="#6c7086" Margin="0,0,0,10" FontStyle="Italic"/>
                                <ItemsControl ItemsSource="{Binding RavageCityPlayerSelections}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                      Margin="0,5,0,5">
                                                <TextBlock Text="{Binding Player.DisplayName}" FontSize="{Binding DataContext.MediumFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                            
                            <!-- Chinese Poker scoring input -->
                            <StackPanel Visibility="{Binding IsChinesePokerContract, Converter={StaticResource BoolToVis}}"
                                        Margin="0,0,0,15">
                                <TextBlock Text="Enter Chinese Poker beats (+6 points per beat)" 
                                           FontSize="{Binding LargeFontSize}" 
                                           FontWeight="Bold" Foreground="#228B22" Margin="0,0,0,15" TextWrapping="Wrap"/>
                                
                                <!-- Scoring mode radio buttons -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <RadioButton x:Name="ScoreBySettingRadio" 
                                                 Content="Score by Setting (Front/Middle/Back)" 
                                                 IsChecked="{Binding ChinesePokerScoreBySetting, Mode=TwoWay}"
                                                 Margin="0,0,20,0" VerticalContentAlignment="Center"/>
                                    <RadioButton x:Name="ScoreByTotalRadio" 
                                                 Content="Score by Total Beats" 
                                                 IsChecked="{Binding ChinesePokerScoreByTotalBeats, Mode=TwoWay}"
                                                 VerticalContentAlignment="Center"/>
                                </StackPanel>
                                
                                <!-- Score by Setting mode: 4 players × 3 columns (Front/Middle/Back) -->
                                <Grid Visibility="{Binding ChinesePokerScoreBySetting, Converter={StaticResource BoolToVis}}" 
                                      Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" MinWidth="100"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Headers -->
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Player" FontWeight="Bold" Margin="0,0,10,5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Front" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Middle" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Back" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                    
                                    <!-- Player 1 -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Players[0].DisplayName}" VerticalAlignment="Center" Margin="0,0,10,5"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="CPSetting00" Tag="0,0" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="1" Grid.Column="2" x:Name="CPSetting01" Tag="0,1" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="1" Grid.Column="3" x:Name="CPSetting02" Tag="0,2" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    
                                    <!-- Player 2 -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding Players[1].DisplayName}" VerticalAlignment="Center" Margin="0,0,10,5"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="CPSetting10" Tag="1,0" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="2" Grid.Column="2" x:Name="CPSetting11" Tag="1,1" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="2" Grid.Column="3" x:Name="CPSetting12" Tag="1,2" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    
                                    <!-- Player 3 -->
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="{Binding Players[2].DisplayName}" VerticalAlignment="Center" Margin="0,0,10,5"/>
                                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="CPSetting20" Tag="2,0" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="3" Grid.Column="2" x:Name="CPSetting21" Tag="2,1" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="3" Grid.Column="3" x:Name="CPSetting22" Tag="2,2" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    
                                    <!-- Player 4 -->
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="{Binding Players[3].DisplayName}" VerticalAlignment="Center" Margin="0,0,10,5"/>
                                    <TextBox Grid.Row="4" Grid.Column="1" x:Name="CPSetting30" Tag="3,0" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="4" Grid.Column="2" x:Name="CPSetting31" Tag="3,1" Width="60" TextAlignment="Center" Margin="5,2"/>
                                    <TextBox Grid.Row="4" Grid.Column="3" x:Name="CPSetting32" Tag="3,2" Width="60" TextAlignment="Center" Margin="5,2"/>
                                </Grid>
                                <TextBlock Visibility="{Binding ChinesePokerScoreBySetting, Converter={StaticResource BoolToVis}}"
                                           Text="Each column (Front/Middle/Back) must sum to 6 beats" 
                                           Foreground="#6c7086" FontStyle="Italic" Margin="0,0,0,10"/>
                                
                                <!-- Score by Total Beats mode: 4 players × 1 column -->
                                <Grid Visibility="{Binding ChinesePokerScoreByTotalBeats, Converter={StaticResource BoolToVis}}" 
                                      Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="15"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <StackPanel Grid.Row="0" Grid.Column="0">
                                        <TextBlock Text="{Binding Players[0].DisplayName}" FontWeight="SemiBold"/>
                                        <TextBox x:Name="CPTotal0" Tag="T0" Width="80" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="2">
                                        <TextBlock Text="{Binding Players[1].DisplayName}" FontWeight="SemiBold"/>
                                        <TextBox x:Name="CPTotal1" Tag="T1" Width="80" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="2" Grid.Column="0">
                                        <TextBlock Text="{Binding Players[2].DisplayName}" FontWeight="SemiBold"/>
                                        <TextBox x:Name="CPTotal2" Tag="T2" Width="80" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="2" Grid.Column="2">
                                        <TextBlock Text="{Binding Players[3].DisplayName}" FontWeight="SemiBold"/>
                                        <TextBox x:Name="CPTotal3" Tag="T3" Width="80" TextAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                                <TextBlock Visibility="{Binding ChinesePokerScoreByTotalBeats, Converter={StaticResource BoolToVis}}"
                                           Text="Total beats across all players must equal 18" 
                                           Foreground="#6c7086" FontStyle="Italic" Margin="0,0,0,10"/>
                            </StackPanel>
                            
                            <!-- Score inputs (hidden for Barbu which only uses dropdown) -->
                            <StackPanel Visibility="{Binding ShowScoreInputs, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{Binding ContractInstructions}" FontSize="{Binding LargeFontSize}" 
                                       FontWeight="Bold" Foreground="#228B22" Margin="0,0,0,15" TextWrapping="Wrap"/>
                            
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="{Binding Players[0].DisplayName}" FontWeight="SemiBold"/>
                                    <TextBox x:Name="ScoreInput0" Tag="0"/>
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="{Binding Players[1].DisplayName}" FontWeight="SemiBold"/>
                                    <TextBox x:Name="ScoreInput1" Tag="1"/>
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="0">
                                    <TextBlock Text="{Binding Players[2].DisplayName}" FontWeight="SemiBold"/>
                                    <TextBox x:Name="ScoreInput2" Tag="2"/>
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="2">
                                    <TextBlock Text="{Binding Players[3].DisplayName}" FontWeight="SemiBold"/>
                                    <TextBox x:Name="ScoreInput3" Tag="3"/>
                                </StackPanel>
                            </Grid>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                <Button Content="Back to Bidding" Command="{Binding BackToBiddingCommand}"
                                        Margin="0,0,15,0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="#6c7086"/>
                                            <Setter Property="Foreground" Value="#cdd6f4"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" Padding="10,5" CornerRadius="3">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="#1e1e2e"/>
                                                    <Setter Property="Background" Value="#89b4fa"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="Submit Score" Click="RecordHand_Click" 
                                        Background="#a6e3a1"/>
                            </StackPanel>
                        </StackPanel>
                        
                        <!-- Phase: Score Summary -->
                        <StackPanel Grid.Row="0" Grid.RowSpan="2"
                                    Visibility="{Binding IsScoreSummary, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Score Summary" FontSize="{Binding XLargeFontSize}" FontWeight="SemiBold" 
                                       Foreground="#f5c2e7" Margin="0,0,0,15"/>
                            
                            <!-- Player Score Tiles - 4 in a row, collapsing to 2x2 (W/N top, E/S bottom) -->
                            <UniformGrid Columns="4" Margin="0,0,0,15" x:Name="ScoreTilesGrid">
                                <!-- Player 1 (West) Tile -->
                                <Border Background="#313244" CornerRadius="10" Padding="20" Margin="0,0,10,10" MinWidth="140">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Players[0].Name}" FontSize="{Binding LargeFontSize}" FontWeight="Bold" Foreground="#89b4fa"/>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Before Doubles" FontSize="{Binding SmallFontSize}" Foreground="#6c7086"/>
                                            <TextBlock Text="{Binding SummaryBaseScores[0]}" FontSize="{Binding XXLargeFontSize}" Foreground="#a6adc8"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Final Score" FontSize="{Binding SmallFontSize}" Foreground="#a6e3a1"/>
                                            <TextBlock Text="{Binding SummaryFinalScores[0]}" FontSize="{Binding HugeFontSize}" FontWeight="Bold" Foreground="#a6e3a1"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Player 2 (North) Tile -->
                                <Border Background="#313244" CornerRadius="10" Padding="20" Margin="0,0,10,10" MinWidth="140">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Players[1].Name}" FontSize="{Binding LargeFontSize}" FontWeight="Bold" Foreground="#89b4fa"/>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Before Doubles" FontSize="{Binding SmallFontSize}" Foreground="#6c7086"/>
                                            <TextBlock Text="{Binding SummaryBaseScores[1]}" FontSize="{Binding XXLargeFontSize}" Foreground="#a6adc8"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Final Score" FontSize="{Binding SmallFontSize}" Foreground="#a6e3a1"/>
                                            <TextBlock Text="{Binding SummaryFinalScores[1]}" FontSize="{Binding HugeFontSize}" FontWeight="Bold" Foreground="#a6e3a1"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Player 3 (East) Tile -->
                                <Border Background="#313244" CornerRadius="10" Padding="20" Margin="0,0,10,10" MinWidth="140">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Players[2].Name}" FontSize="{Binding LargeFontSize}" FontWeight="Bold" Foreground="#89b4fa"/>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Before Doubles" FontSize="{Binding SmallFontSize}" Foreground="#6c7086"/>
                                            <TextBlock Text="{Binding SummaryBaseScores[2]}" FontSize="{Binding XXLargeFontSize}" Foreground="#a6adc8"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Final Score" FontSize="{Binding SmallFontSize}" Foreground="#a6e3a1"/>
                                            <TextBlock Text="{Binding SummaryFinalScores[2]}" FontSize="{Binding HugeFontSize}" FontWeight="Bold" Foreground="#a6e3a1"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Player 4 (South) Tile -->
                                <Border Background="#313244" CornerRadius="10" Padding="20" Margin="0,0,10,10" MinWidth="140">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Players[3].Name}" FontSize="{Binding LargeFontSize}" FontWeight="Bold" Foreground="#89b4fa"/>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Before Doubles" FontSize="{Binding SmallFontSize}" Foreground="#6c7086"/>
                                            <TextBlock Text="{Binding SummaryBaseScores[3]}" FontSize="{Binding XXLargeFontSize}" Foreground="#a6adc8"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,10,0,0">
                                            <TextBlock Text="Final Score" FontSize="{Binding SmallFontSize}" Foreground="#a6e3a1"/>
                                            <TextBlock Text="{Binding SummaryFinalScores[3]}" FontSize="{Binding HugeFontSize}" FontWeight="Bold" Foreground="#a6e3a1"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                            
                            <!-- Buttons -->
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Back" Command="{Binding BackToScoreInputCommand}"
                                        Background="#6c7086" Foreground="#cdd6f4" Margin="0,0,15,0"/>
                                <Button Content="Confirm Scores" Command="{Binding ConfirmSummaryCommand}"
                                        Background="#a6e3a1"/>
                            </StackPanel>
                        </StackPanel>
                        
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Footer -->
            <TextBlock Grid.Row="2" Foreground="#6c7086" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Run Text="Round "/><Run Text="{Binding CurrentHandNumber, Mode=OneWay}"/>
                <Run Text=" of 28 (7 games × 4 players)"/>
            </TextBlock>
                        </Grid>
                    </Border>
                    </ScrollViewer>
                </TabItem>
                
                <!-- ==================== TAB 3: SCORE CARD ==================== -->
                <TabItem Header="Score Card">
                    <Border Background="#1e1e2e" Padding="0,15,0,0" MaxWidth="850" HorizontalAlignment="Center">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Score Card" FontSize="{Binding XXLargeFontSize}" FontWeight="Bold" 
                                           Foreground="#89b4fa" VerticalAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                                    <Border Background="#f9e2af" CornerRadius="8" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock Text="Doubled" Foreground="#a6adc8" FontSize="{Binding MediumFontSize}" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                    <Border Background="#f38ba8" Width="14" Height="14" Margin="0,0,5,0" RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <RotateTransform Angle="45"/>
                                        </Border.RenderTransform>
                                    </Border>
                                    <TextBlock Text="Redoubled" Foreground="#a6adc8" FontSize="{Binding MediumFontSize}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- Fixed Header Row -->
                            <Border Grid.Row="1" Background="#45475a" CornerRadius="5,5,0,0" Padding="10" Margin="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="145"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="" FontWeight="Bold" Foreground="#a6e3a1" FontSize="{Binding BaseFontSize}"/>
                                    <Border Grid.Column="1" Width="1" Background="#6c7086" Margin="5,0"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Players[0].NameWithPosition}" FontWeight="Bold" Foreground="#89b4fa" TextAlignment="Center" FontSize="{Binding BaseFontSize}"/>
                                    <Border Grid.Column="3" Width="1" Background="#6c7086" Margin="5,0"/>
                                    <TextBlock Grid.Column="4" Text="{Binding Players[1].NameWithPosition}" FontWeight="Bold" Foreground="#89b4fa" TextAlignment="Center" FontSize="{Binding BaseFontSize}"/>
                                    <Border Grid.Column="5" Width="1" Background="#6c7086" Margin="5,0"/>
                                    <TextBlock Grid.Column="6" Text="{Binding Players[2].NameWithPosition}" FontWeight="Bold" Foreground="#89b4fa" TextAlignment="Center" FontSize="{Binding BaseFontSize}"/>
                                    <Border Grid.Column="7" Width="1" Background="#6c7086" Margin="5,0"/>
                                    <TextBlock Grid.Column="8" Text="{Binding Players[3].NameWithPosition}" FontWeight="Bold" Foreground="#89b4fa" TextAlignment="Center" FontSize="{Binding BaseFontSize}"/>
                                    <Border Grid.Column="9" Width="1" Background="#6c7086" Margin="5,0"/>
                                    <TextBlock Grid.Column="10" Text="Check" FontWeight="Bold" Foreground="#cba6f7" TextAlignment="Center" FontSize="{Binding BaseFontSize}"/>
                                </Grid>
                            </Border>
                            
                            <!-- Scrollable Dealer Sections -->
                            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Scorecard}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <!-- Dealer Header -->
                                                <Border Background="#585b70" Padding="10,8" Margin="0,10,0,1">
                                                    <TextBlock FontWeight="Bold" Foreground="#a6e3a1" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}"><Run Text="{Binding Dealer.Name, Mode=OneWay}"/><Run Text="'s Contracts"/></TextBlock>
                                                </Border>
                                                
                                                <!-- Rows for this dealer's contracts -->
                                                <ItemsControl ItemsSource="{Binding Rows}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Padding="8,3" Margin="0,0,0,1">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="#313244"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsPlayed}" Value="True">
                                                                                <Setter Property="Background" Value="#3b3d52"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="145"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="120"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="120"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="120"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="120"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="80"/>
                                                                </Grid.ColumnDefinitions>
                                                                
                                                                <!-- Column separators -->
                                                                <Border Grid.Column="1" Width="1" Background="#6c7086" Margin="5,0"/>
                                                                <Border Grid.Column="3" Width="1" Background="#6c7086" Margin="5,0"/>
                                                                <Border Grid.Column="5" Width="1" Background="#6c7086" Margin="5,0"/>
                                                                <Border Grid.Column="7" Width="1" Background="#6c7086" Margin="5,0"/>
                                                                <Border Grid.Column="9" Width="1" Background="#6c7086" Margin="5,0"/>
                                                                    
                                                                    <!-- Game name - clickable to edit if played -->
                                                                    <Button Grid.Column="0" 
                                                                            Command="{Binding DataContext.StartEditScoreCommand, RelativeSource={RelativeSource AncestorType=ItemsControl, AncestorLevel=2}}"
                                                                            CommandParameter="{Binding}"
                                                                            HorizontalContentAlignment="Left"
                                                                            IsEnabled="{Binding IsPlayed}">
                                                                        <Button.Style>
                                                                            <Style TargetType="Button">
                                                                                <Setter Property="Cursor" Value="Arrow"/>
                                                                                <Setter Property="Template">
                                                                                    <Setter.Value>
                                                                                        <ControlTemplate TargetType="Button">
                                                                                            <ContentPresenter/>
                                                                                        </ControlTemplate>
                                                                                    </Setter.Value>
                                                                                </Setter>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding IsPlayed}" Value="True">
                                                                                        <Setter Property="Cursor" Value="Hand"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Button.Style>
                                                                        <TextBlock Text="{Binding GameName}" VerticalAlignment="Center" Foreground="#89b4fa" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding IsPlayed}" Value="True">
                                                                                            <Setter Property="TextDecorations" Value="Underline"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </Button>
                                                                    
                                                                    <!-- Player 1 Score -->
                                                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,3,0">
                                                                            <!-- Doubles (yellow circles) -->
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[0].DoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Border Background="#f9e2af" CornerRadius="{Binding DataContext.CircleCornerRadius, RelativeSource={RelativeSource AncestorType=Window}}" Width="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0">
                                                                                            <TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.CircleFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" 
                                                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                        </Border>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                            <!-- Redoubles (red diamonds) -->
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[0].RedoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Grid Width="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0"><Border Background="#f38ba8" Width="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" RenderTransformOrigin="0.5,0.5"><Border.RenderTransform><RotateTransform Angle="45"/></Border.RenderTransform></Border><TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.DiamondFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Grid>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                        </StackPanel>
                                                                        <TextBlock Text="{Binding PlayerCells[0].Score}" TextAlignment="Center" VerticalAlignment="Center" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Foreground" Value="#6c7086"/>
                                                                                    <Style.Triggers>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[0].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[0].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="True"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#a6e3a1"/>
                                                                                        </MultiDataTrigger>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[0].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[0].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="False"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#f38ba8"/>
                                                                                        </MultiDataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </StackPanel>
                                                                    
                                                                    <!-- Player 2 Score -->
                                                                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,3,0">
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[1].DoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Border Background="#f9e2af" CornerRadius="{Binding DataContext.CircleCornerRadius, RelativeSource={RelativeSource AncestorType=Window}}" Width="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0">
                                                                                            <TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.CircleFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" 
                                                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                        </Border>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[1].RedoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Grid Width="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0"><Border Background="#f38ba8" Width="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" RenderTransformOrigin="0.5,0.5"><Border.RenderTransform><RotateTransform Angle="45"/></Border.RenderTransform></Border><TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.DiamondFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Grid>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                        </StackPanel>
                                                                        <TextBlock Text="{Binding PlayerCells[1].Score}" TextAlignment="Center" VerticalAlignment="Center" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Foreground" Value="#6c7086"/>
                                                                                    <Style.Triggers>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[1].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[1].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="True"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#a6e3a1"/>
                                                                                        </MultiDataTrigger>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[1].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[1].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="False"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#f38ba8"/>
                                                                                        </MultiDataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </StackPanel>
                                                                    
                                                                    <!-- Player 3 Score -->
                                                                    <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,3,0">
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[2].DoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Border Background="#f9e2af" CornerRadius="{Binding DataContext.CircleCornerRadius, RelativeSource={RelativeSource AncestorType=Window}}" Width="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0">
                                                                                            <TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.CircleFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" 
                                                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                        </Border>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[2].RedoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Grid Width="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0"><Border Background="#f38ba8" Width="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" RenderTransformOrigin="0.5,0.5"><Border.RenderTransform><RotateTransform Angle="45"/></Border.RenderTransform></Border><TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.DiamondFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Grid>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                        </StackPanel>
                                                                        <TextBlock Text="{Binding PlayerCells[2].Score}" TextAlignment="Center" VerticalAlignment="Center" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Foreground" Value="#6c7086"/>
                                                                                    <Style.Triggers>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[2].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[2].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="True"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#a6e3a1"/>
                                                                                        </MultiDataTrigger>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[2].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[2].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="False"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#f38ba8"/>
                                                                                        </MultiDataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </StackPanel>
                                                                    
                                                                    <!-- Player 4 Score -->
                                                                    <StackPanel Grid.Column="8" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,3,0">
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[3].DoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Border Background="#f9e2af" CornerRadius="{Binding DataContext.CircleCornerRadius, RelativeSource={RelativeSource AncestorType=Window}}" Width="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.CircleSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0">
                                                                                            <TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.CircleFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" 
                                                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                        </Border>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                            <ItemsControl ItemsSource="{Binding PlayerCells[3].RedoubledTargets}">
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Grid Width="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondOuterSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="1,0"><Border Background="#f38ba8" Width="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.DiamondInnerSize, RelativeSource={RelativeSource AncestorType=Window}}" RenderTransformOrigin="0.5,0.5"><Border.RenderTransform><RotateTransform Angle="45"/></Border.RenderTransform></Border><TextBlock Text="{Binding}" Foreground="#1e1e2e" FontSize="{Binding DataContext.DiamondFontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Grid>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                        </StackPanel>
                                                                        <TextBlock Text="{Binding PlayerCells[3].Score}" TextAlignment="Center" VerticalAlignment="Center" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Foreground" Value="#6c7086"/>
                                                                                    <Style.Triggers>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[3].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[3].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="True"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#a6e3a1"/>
                                                                                        </MultiDataTrigger>
                                                                                        <MultiDataTrigger>
                                                                                            <MultiDataTrigger.Conditions>
                                                                                                <Condition Binding="{Binding PlayerCells[3].HasScore}" Value="True"/>
                                                                                                <Condition Binding="{Binding PlayerCells[3].Score, Converter={x:Static local:ScoreSignConverter.Instance}}" Value="False"/>
                                                                                            </MultiDataTrigger.Conditions>
                                                                                            <Setter Property="Foreground" Value="#f38ba8"/>
                                                                                        </MultiDataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </StackPanel>
                                                                    
                                                                    <!-- Check Sum -->
                                                                    <TextBlock Grid.Column="10" Text="{Binding CheckSumDisplay}" TextAlignment="Center" VerticalAlignment="Center"
                                                                               Foreground="#cba6f7" FontWeight="SemiBold" FontSize="{Binding DataContext.BaseFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </TabItem>
            </TabControl>
            
            <!-- Restart Bidding Confirmation Popup -->
            <Grid Visibility="{Binding ShowRestartBiddingConfirm, Converter={StaticResource BoolToVis}}">
                <Border Background="#80000000"/>
                <Border Background="#313244" CornerRadius="12" Padding="30" 
                        VerticalAlignment="Center" HorizontalAlignment="Center"
                        MinWidth="350"
                        BorderBrush="#45475a" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Restart Bidding?" FontSize="{Binding XXLargeFontSize}" FontWeight="Bold" 
                                   Foreground="#f9e2af" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <TextBlock Text="Are you sure you want to start the bidding over? All doubles for this round will be reset." 
                                   Foreground="#cdd6f4" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="Yes, Restart" Command="{Binding ConfirmRestartBiddingCommand}"
                                    Background="#f38ba8" Foreground="#1e1e2e" FontWeight="Bold"
                                    Padding="20,10" Margin="0,0,15,0"/>
                            <Button Content="Cancel" Command="{Binding CancelRestartBiddingCommand}"
                                    Background="#6c7086" Foreground="#cdd6f4" FontWeight="Bold"
                                    Padding="20,10"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
        
        <!-- Settings Panel Overlay (global overlay) -->
        <Grid Visibility="{Binding IsSettingsOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="#80000000"/>
            <Border Background="#313244" CornerRadius="12" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    Width="780" Height="520"
                    BorderBrush="#45475a" BorderThickness="1">
                <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Settings Sidebar -->
                        <Border Grid.Column="0" Background="#45475a" CornerRadius="12,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Settings Header -->
                                <TextBlock Grid.Row="0" Text="Settings" FontSize="{Binding XLargeFontSize}" FontWeight="Bold" 
                                           Foreground="#cdd6f4" Margin="20,20,20,15"/>
                                
                                <!-- Settings Tab Buttons -->
                                <StackPanel Grid.Row="1">
                                    <Button Content="Text Size" 
                                            Command="{Binding SelectSettingsTabCommand}" CommandParameter="0"
                                            Background="{Binding SelectedSettingsTab, Converter={StaticResource TabSelectedConverter}, ConverterParameter=0}"
                                            Style="{StaticResource SettingsTabButtonStyle}"/>
                                    <Button Content="Game Variants" 
                                            Command="{Binding SelectSettingsTabCommand}" CommandParameter="1"
                                            Background="{Binding SelectedSettingsTab, Converter={StaticResource TabSelectedConverter}, ConverterParameter=1}"
                                            Style="{StaticResource SettingsTabButtonStyle}"/>
                                    <Button Content="Optional Rules" 
                                            Command="{Binding SelectSettingsTabCommand}" CommandParameter="2"
                                            Background="{Binding SelectedSettingsTab, Converter={StaticResource TabSelectedConverter}, ConverterParameter=2}"
                                            Style="{StaticResource SettingsTabButtonStyle}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <!-- Settings Content Area -->
                        <Grid Grid.Column="1" Margin="25">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Tab Content -->
                            <Grid Grid.Row="0">
                                <!-- Tab 0: Text Size -->
                                <StackPanel Visibility="{Binding SelectedSettingsTab, Converter={StaticResource TabVisibilityConverter}, ConverterParameter=0}">
                                    <TextBlock Text="Text Size" FontSize="{Binding XLargeFontSize}" FontWeight="Bold" 
                                               Foreground="#cdd6f4" Margin="0,0,0,15"/>
                                    <TextBlock Text="Choose a text size for the application." Foreground="#a6adc8" Margin="0,0,0,20"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <RadioButton GroupName="TextSize" Content="Small (Default)"
                                                     IsChecked="{Binding SelectedTextSize, Converter={StaticResource EnumBoolConverter}, ConverterParameter=Small}"
                                                     Foreground="#cdd6f4" VerticalContentAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <RadioButton GroupName="TextSize" Content="Medium"
                                                     IsChecked="{Binding SelectedTextSize, Converter={StaticResource EnumBoolConverter}, ConverterParameter=Medium}"
                                                     Foreground="#cdd6f4" VerticalContentAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <RadioButton GroupName="TextSize" Content="Large"
                                                     IsChecked="{Binding SelectedTextSize, Converter={StaticResource EnumBoolConverter}, ConverterParameter=Large}"
                                                     Foreground="#cdd6f4" VerticalContentAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Tab 1: Game Variants -->
                                <ScrollViewer Visibility="{Binding SelectedSettingsTab, Converter={StaticResource TabVisibilityConverter}, ConverterParameter=1}"
                                              VerticalScrollBarVisibility="Auto">
                                    <StackPanel>
                                        <TextBlock Text="Game Variants" FontSize="{Binding XLargeFontSize}" FontWeight="Bold" 
                                                   Foreground="#cdd6f4" Margin="0,0,0,12"/>
                                        
                                        <TextBlock Text="Game Mode" FontSize="{Binding LargeFontSize}" FontWeight="SemiBold" Foreground="#89b4fa" Margin="0,0,0,6"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <RadioButton Content="Standard" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}" Margin="0,0,20,0"
                                                         IsChecked="{Binding IsStandardMode, Mode=TwoWay}" GroupName="GameMode"/>
                                            <RadioButton Content="Salade" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}"
                                                         IsChecked="{Binding IsSaladeMode, Mode=TwoWay}" GroupName="GameMode"/>
                                        </StackPanel>
                                        
                                        <TextBlock Text="Contract/Game Names" FontSize="{Binding LargeFontSize}" FontWeight="SemiBold" Foreground="#89b4fa" Margin="0,0,0,6"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <RadioButton Content="Classic" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}" Margin="0,0,20,0"
                                                         IsChecked="{Binding IsClassicNames, Mode=TwoWay}" GroupName="ContractNames"/>
                                            <RadioButton Content="Modern" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}"
                                                         IsChecked="{Binding IsModernNames, Mode=TwoWay}" GroupName="ContractNames"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,0,0,10">
                                            <TextBlock Foreground="#6c7086" FontSize="{Binding MediumFontSize}" TextWrapping="Wrap">
                                                <Run Text="Classic: Nullo, No Queens, Hearts, No Last Two, Barbu, Trumps, Fan Tan"/>
                                            </TextBlock>
                                            <TextBlock Foreground="#6c7086" FontSize="{Binding MediumFontSize}" TextWrapping="Wrap">
                                                <Run Text="Modern: No Tricks, No Queens, No Hearts, No Last Two, No King of Hearts, Trumps, Domino"/>
                                            </TextBlock>
                                        </StackPanel>
                                        
                                        <TextBlock Text="Fan Tan/Domino Scoring" FontSize="{Binding LargeFontSize}" FontWeight="SemiBold" Foreground="#89b4fa" Margin="0,0,0,6"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Text="{Binding FanTanScoringDisplay}" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}" 
                                                       VerticalAlignment="Center" Margin="0,0,15,0"/>
                                            <Button Content="Edit" Command="{Binding OpenFanTanScoringEditorCommand}"
                                                    Style="{StaticResource SettingsEditButtonStyle}"/>
                                        </StackPanel>
                                        
                                        <TextBlock Text="Optional Contracts/Games (Standard Barbu only)" FontSize="{Binding LargeFontSize}" FontWeight="SemiBold" Foreground="#89b4fa" Margin="0,0,0,6"/>
                                        <StackPanel Margin="0,0,0,6">
                                            <CheckBox Content="Ravage City (8th contract)" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}"
                                                      IsChecked="{Binding RavageCityEnabled, Mode=TwoWay}" IsEnabled="{Binding NotSaladeMode}"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,0,0,6">
                                            <CheckBox Content="Chinese Poker (9th contract)" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}"
                                                      IsChecked="{Binding ChinesePokerEnabled, Mode=TwoWay}" IsEnabled="{Binding NotSaladeMode}"/>
                                        </StackPanel>
                                        <TextBlock Foreground="#6c7086" FontSize="{Binding MediumFontSize}" Margin="0,0,0,0" TextWrapping="Wrap"
                                                   Visibility="{Binding IsSaladeMode, Converter={StaticResource BoolToVis}}">
                                            <Run Text="Optional contracts are not available in Salade mode."/>
                                        </TextBlock>
                                    </StackPanel>
                                </ScrollViewer>
                                
                                <!-- Tab 2: Optional Rules -->
                                <StackPanel Visibility="{Binding SelectedSettingsTab, Converter={StaticResource TabVisibilityConverter}, ConverterParameter=2}">
                                    <TextBlock Text="Optional Rules" FontSize="{Binding XLargeFontSize}" FontWeight="Bold" 
                                               Foreground="#cdd6f4" Margin="0,0,0,15"/>
                                    
                                    <TextBlock Text="Doubling Rules" FontSize="{Binding LargeFontSize}" FontWeight="SemiBold" Foreground="#89b4fa" Margin="0,0,0,8"/>
                                    <StackPanel>
                                        <CheckBox Content="Dealer allowed to double" Foreground="#cdd6f4" FontSize="{Binding BaseFontSize}"
                                                  IsChecked="{Binding DealerAllowedToDouble, Mode=TwoWay}"/>
                                        <TextBlock Text="When unchecked, the dealer's turn is skipped entirely during bidding." 
                                                   Foreground="#6c7086" FontSize="{Binding MediumFontSize}" Margin="25,5,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Close Button -->
                            <Button Grid.Row="1" Content="Save and Close" Command="{Binding CloseSettingsCommand}"
                                    Background="#89b4fa" Foreground="#1e1e2e" FontWeight="Bold"
                                    Padding="20,10" HorizontalAlignment="Right"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        
        <!-- Fan Tan Scoring Editor Popup -->
        <Grid Visibility="{Binding ShowFanTanScoringEditor, Converter={StaticResource BoolToVis}}">
            <Border Background="#80000000"/>
            <Border Background="#313244" CornerRadius="12" Padding="25" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    MinWidth="350"
                    BorderBrush="#45475a" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Edit Fan Tan/Domino Scoring" FontSize="{Binding XLargeFontSize}" FontWeight="Bold" 
                               Foreground="#cdd6f4" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="Scores must total 65 points" Foreground="#a6adc8" 
                               HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Column="0" Grid.Row="0" Text="1st" Foreground="#cdd6f4" 
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Grid.Column="1" Grid.Row="0" Text="2nd" Foreground="#cdd6f4" 
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Grid.Column="2" Grid.Row="0" Text="3rd" Foreground="#cdd6f4" 
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Grid.Column="3" Grid.Row="0" Text="4th" Foreground="#cdd6f4" 
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        
                        <TextBox Grid.Column="0" Grid.Row="1" Text="{Binding TempFanTanScore1st, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="60" HorizontalAlignment="Center" TextAlignment="Center"
                                 Background="#45475a" Foreground="#cdd6f4" BorderThickness="1" BorderBrush="#585b70" Margin="5"/>
                        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding TempFanTanScore2nd, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="60" HorizontalAlignment="Center" TextAlignment="Center"
                                 Background="#45475a" Foreground="#cdd6f4" BorderThickness="1" BorderBrush="#585b70" Margin="5"/>
                        <TextBox Grid.Column="2" Grid.Row="1" Text="{Binding TempFanTanScore3rd, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="60" HorizontalAlignment="Center" TextAlignment="Center"
                                 Background="#45475a" Foreground="#cdd6f4" BorderThickness="1" BorderBrush="#585b70" Margin="5"/>
                        <TextBox Grid.Column="3" Grid.Row="1" Text="{Binding TempFanTanScore4th, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="60" HorizontalAlignment="Center" TextAlignment="Center"
                                 Background="#45475a" Foreground="#cdd6f4" BorderThickness="1" BorderBrush="#585b70" Margin="5"/>
                    </Grid>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <TextBlock Text="Current total: " Foreground="#a6adc8"/>
                        <TextBlock Text="{Binding TempFanTanScoringTotal}" 
                                   Foreground="{Binding IsTempFanTanScoringValid, Converter={StaticResource BoolToColorConverter}}"/>
                        <TextBlock Text=" (must equal 65)" Foreground="#a6adc8"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Save" Command="{Binding SaveFanTanScoringCommand}"
                                Background="#89b4fa" Foreground="#1e1e2e" FontWeight="Bold"
                                Padding="20,8" Margin="0,0,10,0" BorderThickness="0"
                                IsEnabled="{Binding IsTempFanTanScoringValid}"/>
                        <Button Content="Discard" Command="{Binding DiscardFanTanScoringCommand}"
                                Background="#585b70" Foreground="#cdd6f4"
                                Padding="20,8" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Load Game Picker Popup (global overlay) -->
        <Grid Visibility="{Binding ShowLoadGamePicker, Converter={StaticResource BoolToVis}}">
            <Border Background="#80000000"/>
            <Border Background="#313244" CornerRadius="12" Padding="30" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    MinWidth="400" MaxHeight="500"
                    BorderBrush="#45475a" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Load Game" FontSize="{Binding XXLargeFontSize}" FontWeight="Bold" 
                               Foreground="#89b4fa" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="Select a saved game to continue:" 
                               Foreground="#a6adc8" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    
                    <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SavedGames}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Main save game button -->
                                        <Button Grid.Column="0"
                                                Command="{Binding DataContext.SelectSavedGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Padding="15,12"
                                                HorizontalContentAlignment="Left">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="#45475a"/>
                                                    <Setter Property="Foreground" Value="#cdd6f4"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Foreground" Value="#1e1e2e"/>
                                                            <Setter Property="Background" Value="#89b4fa"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" 
                                                           FontSize="{Binding DataContext.LargeFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                    <TextBlock Text="{Binding RoundInfo}" 
                                                               FontSize="{Binding DataContext.SmallFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                    <TextBlock Text=" • " 
                                                               FontSize="{Binding DataContext.SmallFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                    <TextBlock Text="{Binding SavedAtDisplay}" 
                                                               FontSize="{Binding DataContext.SmallFontSize, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Button>
                                        
                                        <!-- Ellipsis menu button (hidden when delete is showing) -->
                                        <Button Grid.Column="1" Content="⋮" 
                                                Command="{Binding DataContext.ToggleSaveDeleteMenuCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Background="#45475a" Foreground="#a6adc8"
                                                FontSize="18" FontWeight="Bold"
                                                Padding="8,12" Margin="2,0,0,0"
                                                VerticalAlignment="Stretch"
                                                Visibility="{Binding IsShowingDeleteOption, Converter={StaticResource InverseBoolToVis}}"/>
                                        
                                        <!-- Delete button (replaces ellipsis when showing) -->
                                        <Button Grid.Column="1" Content="Delete" 
                                                Command="{Binding DataContext.DeleteSavedGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Background="#f38ba8" Foreground="#1e1e2e"
                                                FontWeight="Bold" Padding="12,8" Margin="2,0,0,0"
                                                VerticalAlignment="Stretch"
                                                Visibility="{Binding IsShowingDeleteOption, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <Button Content="Cancel" Command="{Binding CancelLoadGameCommand}"
                            Background="#6c7086" Foreground="#cdd6f4" FontWeight="Bold"
                            Padding="20,10" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Save Confirmation Popup (global overlay) -->
        <Grid Visibility="{Binding ShowSaveConfirmation, Converter={StaticResource BoolToVis}}">
            <Border Background="#80000000"/>
            <Border Background="#313244" CornerRadius="12" Padding="30" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    MinWidth="300"
                    BorderBrush="#45475a" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="✓" FontSize="48" FontWeight="Bold" 
                               Foreground="#a6e3a1" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Game successfully saved" FontSize="{Binding LargeFontSize}" FontWeight="Bold" 
                               Foreground="#cdd6f4" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <Button Content="OK" Command="{Binding DismissSaveConfirmationCommand}"
                            Background="#a6e3a1" Foreground="#1e1e2e" FontWeight="Bold"
                            Padding="30,10" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- New Game Confirmation Popup (global overlay) -->
        <Grid Visibility="{Binding ShowNewGameConfirm, Converter={StaticResource BoolToVis}}">
            <Border Background="#80000000"/>
            <Border Background="#313244" CornerRadius="12" Padding="30" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    MinWidth="350"
                    BorderBrush="#45475a" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="New Game?" FontSize="{Binding XXLargeFontSize}" FontWeight="Bold" 
                               Foreground="#f38ba8" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="Are you sure? Unsaved progress will be lost." 
                               Foreground="#cdd6f4" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Yes, New Game" Command="{Binding ConfirmNewGameCommand}"
                                Background="#f38ba8" Foreground="#1e1e2e" FontWeight="Bold"
                                Padding="20,10" Margin="0,0,15,0"/>
                        <Button Content="Cancel" Command="{Binding CancelNewGameCommand}"
                                Background="#6c7086" Foreground="#cdd6f4" FontWeight="Bold"
                                Padding="20,10"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Settings Changed New Game Prompt (global overlay) -->
        <Grid Visibility="{Binding ShowSettingsNewGamePrompt, Converter={StaticResource BoolToVis}}">
            <Border Background="#80000000"/>
            <Border Background="#313244" CornerRadius="12" Padding="30" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    MinWidth="400"
                    BorderBrush="#45475a" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Settings Changed" FontSize="{Binding XXLargeFontSize}" FontWeight="Bold" 
                               Foreground="#f9e2af" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="You've changed game settings that affect scoring. Would you like to start a new game with these settings?" 
                               Foreground="#cdd6f4" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,10" MaxWidth="350"/>
                    <TextBlock Text="Your current game will not be affected if you continue." 
                               Foreground="#a6adc8" FontSize="{Binding SmallFontSize}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Start New Game" Command="{Binding ConfirmSettingsNewGameCommand}"
                                Background="#a6e3a1" Foreground="#1e1e2e" FontWeight="Bold"
                                Padding="20,10" Margin="0,0,15,0"/>
                        <Button Content="Continue Current" Command="{Binding CancelSettingsNewGameCommand}"
                                Background="#6c7086" Foreground="#cdd6f4" FontWeight="Bold"
                                Padding="20,10"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Edit Score Popup (global overlay) -->
        <Grid Visibility="{Binding IsEditingScore, Converter={StaticResource BoolToVis}}"
              x:Name="EditScorePopup" IsVisibleChanged="EditScorePopup_IsVisibleChanged">
            <Border Background="#80000000"/>
            <Border Background="#313244" CornerRadius="12" Padding="30" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    MinWidth="400" MaxWidth="600"
                    BorderBrush="#45475a" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Edit Score" FontSize="{Binding HugeFontSize}" FontWeight="Bold" 
                               Foreground="#cdd6f4" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding EditingGameDescription}" FontSize="{Binding LargeFontSize}" 
                               Foreground="#a6adc8" HorizontalAlignment="Center" Margin="0,0,0,20"
                               TextWrapping="Wrap" TextAlignment="Center"/>
                    
                    <!-- Score Entry Grid -->
                    <Grid HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Player Names -->
                        <TextBlock Grid.Column="0" Grid.Row="0" Text="{Binding Players[0].Name}" 
                                   Foreground="#cdd6f4" HorizontalAlignment="Center" Margin="10,0,10,5"/>
                        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Players[1].Name}" 
                                   Foreground="#cdd6f4" HorizontalAlignment="Center" Margin="10,0,10,5"/>
                        <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding Players[2].Name}" 
                                   Foreground="#cdd6f4" HorizontalAlignment="Center" Margin="10,0,10,5"/>
                        <TextBlock Grid.Column="3" Grid.Row="0" Text="{Binding Players[3].Name}" 
                                   Foreground="#cdd6f4" HorizontalAlignment="Center" Margin="10,0,10,5"/>
                        
                        <!-- Score Inputs -->
                        <TextBox Grid.Column="0" Grid.Row="1" x:Name="EditInput1" Width="70" 
                                 Background="#45475a" Foreground="#cdd6f4" FontSize="{Binding LargeFontSize}"
                                 TextAlignment="Center" Padding="5" Margin="10,0"/>
                        <TextBox Grid.Column="1" Grid.Row="1" x:Name="EditInput2" Width="70" 
                                 Background="#45475a" Foreground="#cdd6f4" FontSize="{Binding LargeFontSize}"
                                 TextAlignment="Center" Padding="5" Margin="10,0"/>
                        <TextBox Grid.Column="2" Grid.Row="1" x:Name="EditInput3" Width="70" 
                                 Background="#45475a" Foreground="#cdd6f4" FontSize="{Binding LargeFontSize}"
                                 TextAlignment="Center" Padding="5" Margin="10,0"/>
                        <TextBox Grid.Column="3" Grid.Row="1" x:Name="EditInput4" Width="70" 
                                 Background="#45475a" Foreground="#cdd6f4" FontSize="{Binding LargeFontSize}"
                                 TextAlignment="Center" Padding="5" Margin="10,0"/>
                    </Grid>
                    
                    <!-- Error Message -->
                    <TextBlock Text="{Binding EditErrorMessage}" Foreground="#f38ba8" 
                               TextAlignment="Center" Margin="0,15,0,0"
                               Visibility="{Binding HasEditError, Converter={StaticResource BoolToVis}}"
                               TextWrapping="Wrap"/>
                    
                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button Content="Confirm" Click="EditScore_Click"
                                Background="#a6e3a1" Foreground="#1e1e2e" FontWeight="Bold"
                                Padding="20,10" Margin="0,0,15,0" Cursor="Hand"/>
                        <Button Content="Cancel" Command="{Binding CancelEditScoreCommand}"
                                Background="#6c7086" Foreground="#cdd6f4" FontWeight="Bold"
                                Padding="20,10" Cursor="Hand"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>

